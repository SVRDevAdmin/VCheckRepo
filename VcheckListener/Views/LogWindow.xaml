<Window x:Class="VCheckListener.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VCheckListener.Views"
        mc:Ignorable="d"        
        Title="Logs" Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="LogList"
                 FontFamily="Consolas"
                 FontSize="13"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Top"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="False">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <!-- Item content -->
                        <TextBlock Text="{Binding}" Padding="5"/>
                        <!-- Separator line -->
                        <Border Height="0.5" Background="Black" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

        <Button Grid.Row="1" Name="Clear_Button"
                Margin="0,8,0,0"
                Padding="6,2"
                HorizontalAlignment="Right"
                Click="ClearLogs_Click"/>
    </Grid>
</Window>
